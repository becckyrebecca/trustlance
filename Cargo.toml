[workspace]
resolver = "2"
members = [
    "contracts/trustlance-escrow-contract",
]

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
panic = "abort"
lto = true
codegen-units = 1
